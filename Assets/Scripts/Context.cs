using UnityEngine;
using System.Collections;

public class Context : MonoBehaviour {

    // Static variables to track in-game information

    // Tracks player lives
    public static int PlayerLives = 3;

    // JT TODO: Create class to hold letters needed and how many of them
    // Using class (above), have a variable that keeps track of each letter and how many of them have been collected (i.e. put in array)

}
